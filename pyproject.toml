[project]
name = "netprobe"
version = "1.0.0"
description = "UDP tabanli guvenilir dosya aktarimi, trafik izleme ve ag performans analiz platformu"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
